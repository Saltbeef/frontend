name: Verify API Secrets

# This workflow verifies that API secrets are configured correctly
# Run manually after adding secrets to test connectivity

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Check secrets are set
        run: |
          echo "Checking if secrets are configured..."
          echo ""
          echo "Context information:"
          echo "  Repository: ${{ github.repository }}"
          echo "  Ref: ${{ github.ref }}"
          echo "  Event: ${{ github.event_name }}"
          echo ""

          # Check APIFY_API_TOKEN
          APIFY_TOKEN="${{ secrets.APIFY_API_TOKEN }}"
          if [ -z "$APIFY_TOKEN" ]; then
            echo "❌ APIFY_API_TOKEN is not set or empty"
            echo "   Please add it at: https://github.com/${{ github.repository }}/settings/secrets/actions"
            exit 1
          else
            TOKEN_LENGTH=${#APIFY_TOKEN}
            echo "✅ APIFY_API_TOKEN is set ($TOKEN_LENGTH characters)"
          fi

          # Check ANTHROPIC_API_KEY
          ANTHROPIC_KEY="${{ secrets.ANTHROPIC_API_KEY }}"
          if [ -z "$ANTHROPIC_KEY" ]; then
            echo "❌ ANTHROPIC_API_KEY is not set or empty"
            echo "   Please add it at: https://github.com/${{ github.repository }}/settings/secrets/actions"
            exit 1
          else
            KEY_LENGTH=${#ANTHROPIC_KEY}
            echo "✅ ANTHROPIC_API_KEY is set ($KEY_LENGTH characters)"
          fi

      - name: Verify Apify API Connection
        env:
          APIFY_API_TOKEN: ${{ secrets.APIFY_API_TOKEN }}
        run: |
          python3 -c "
          import os
          import json
          import urllib.request

          token = os.getenv('APIFY_API_TOKEN')
          url = f'https://api.apify.com/v2/users/me?token={token}'

          try:
              with urllib.request.urlopen(url, timeout=10) as response:
                  data = json.loads(response.read().decode('utf-8'))
                  username = data.get('data', {}).get('username', 'Unknown')
                  print(f'✅ Apify API verified! User: {username}')
          except Exception as e:
              print(f'❌ Apify API test failed: {e}')
              exit(1)
          "

      - name: Verify Anthropic API Connection
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          python3 -c "
          import os
          import json
          import urllib.request

          api_key = os.getenv('ANTHROPIC_API_KEY')
          url = 'https://api.anthropic.com/v1/messages'

          headers = {
              'x-api-key': api_key,
              'anthropic-version': '2023-06-01',
              'content-type': 'application/json'
          }

          body = {
              'model': 'claude-sonnet-4-5-20250929',
              'max_tokens': 10,
              'messages': [{'role': 'user', 'content': 'Hi'}]
          }

          try:
              req = urllib.request.Request(
                  url,
                  data=json.dumps(body).encode('utf-8'),
                  headers=headers,
                  method='POST'
              )

              with urllib.request.urlopen(req, timeout=30) as response:
                  data = json.loads(response.read().decode('utf-8'))
                  model = data.get('model', 'Unknown')
                  print(f'✅ Anthropic API verified! Model: {model}')
          except Exception as e:
              print(f'❌ Anthropic API test failed: {e}')
              exit(1)
          "

      - name: Success Summary
        run: |
          echo "🎉 All API secrets verified successfully!"
          echo ""
          echo "Next steps:"
          echo "  1. Run 'Analyze House' workflow with a test house"
          echo "  2. Check results in houses/ directory"
          echo "  3. View generated HTML reports"
